/**
 * Public HTTPS URL of the separate OTA bundle Git repository.
 * Replace this with your repo before shipping a release build.
 */
export const OTA_GIT_URL = 'https://github.com/AnirudhKudikala/OTA-bundle.git';
